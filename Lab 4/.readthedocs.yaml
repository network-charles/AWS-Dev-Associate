# Required
version: 2

# Set the OS, Python version and other tools you might need
build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

# Build documentation in the "docs/" directory with Sphinx
sphinx:
  configuration: Lab 4/docs/conf.py

formats: all

python:
  install:
  - requirements: Lab 4/docs/requirements.txt
